pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = 'terrarium'

include 'core'
include 'lwjgl3'

// Android module. Requires the Android SDK; the path is set in android/local.properties.
include 'android'
